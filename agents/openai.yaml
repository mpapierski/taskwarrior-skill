interface:
  display_name: "Taskwarrior"
  short_description: "Manage, configure, and troubleshoot Taskwarrior"
  default_prompt: "Use $taskwarrior to manage or explain Taskwarrior with official-docs-based references, using the direct `task` CLI when task or config state is relevant."
